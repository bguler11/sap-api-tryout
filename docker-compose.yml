version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sap-tryout-backend
    environment:
      - PORT=3001
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-sap-api-tryout-default-key-32chr!}
      - NODE_ENV=production
    volumes:
      - sap_data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: sap-tryout-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  sap_data:
